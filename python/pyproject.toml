[project]
name = "tools"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "GitPython",
]

[project.scripts]
download-dependencies = "tools.download_dependencies:main"
build-shaders = "tools.build_shaders:main"
